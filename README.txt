1. Необходимо собрать с помощью Maven: mvn clean compile assebly:single
2. Запусть через терминал: java -jar AppReport-jar-with-dependencies.jar ${oldDir} ${newDir} 
3. Ввести email получателя

${oldDir} ${newDir} - можно взять в тестах 	